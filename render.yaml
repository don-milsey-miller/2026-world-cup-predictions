services:
  - type: web
    name: worldcup-match-predictor
    runtime: python
    buildCommand: pip install -e .
    startCommand: uvicorn worldcup_predictor.app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.8
